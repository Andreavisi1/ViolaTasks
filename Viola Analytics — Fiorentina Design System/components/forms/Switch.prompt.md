Boolean toggle. The canonical use is the "Normalizza per 90′" switch with an explanatory hint.

```jsx
<Switch checked={per90} onChange={setPer90}
  label="Normalizza per 90′"
  hint="Divide i conteggi per i minuti × 90. Le percentuali non si normalizzano." />
```
